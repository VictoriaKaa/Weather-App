import { GmtOffsetPipe } from './gmt-offset.pipe';

describe('GmtOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new GmtOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
